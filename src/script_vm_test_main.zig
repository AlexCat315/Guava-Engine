const vm = @import("engine/script/vm.zig");
const runtime = @import("engine/script/runtime.zig");
const wasm_vm = @import("engine/script/wasm_vm.zig");

test {
    _ = vm;
    _ = runtime;
    _ = wasm_vm;
}
