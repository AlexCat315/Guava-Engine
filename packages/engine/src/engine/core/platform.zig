const platform = @import("guava_rhi").platform;

pub const Platform = platform.Platform;
pub const detect = platform.detect;
pub const fromTarget = platform.fromTarget;
pub const fromOsTag = platform.fromOsTag;
pub const name = platform.name;
